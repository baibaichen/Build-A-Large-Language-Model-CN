# 交叉熵计算公式

交叉熵（Cross Entropy）是衡量两个概率分布差异的指标，在深度学习中尤其常用于分类和语言模型的损失函数。以下是交叉熵的计算公式：

## 基本公式

对于离散概率分布，交叉熵公式为：

$$H(y, \hat{y}) = -\sum_{i} y_i \log(\hat{y}_i)$$

其中：
- $y_i$ 是真实分布（通常是one-hot向量）
- $\hat{y}_i$ 是预测分布（模型输出的概率）

## 语言模型中的交叉熵

在语言模型中，交叉熵通常计算为：

$$H = -\frac{1}{N}\sum_{i=1}^{N} \log(p(w_i|w_1, w_2, ..., w_{i-1}))$$

其中：
- $N$ 是序列中的单词数量
- $p(w_i|w_1, w_2, ..., w_{i-1})$ 是模型预测下一个单词 $w_i$ 的概率，基于之前所有单词

## PyTorch 中的实现

在代码示例中，以下步骤等效于计算交叉熵：

1. 提取目标标记的概率：`target_probas_1`, `target_probas_2`
2. 计算对数概率：`log_probas = torch.log(torch.cat((target_probas_1, target_probas_2)))`
3. 计算平均值并取负：`neg_avg_log_probas = avg_log_probas * -1`

这对应于交叉熵公式的平均负对数似然形式。

## 优化目标

训练语言模型时，我们的目标是最小化这个交叉熵值，使模型预测的概率分布尽可能接近真实分布，从而提高模型生成连贯、合理文本的能力。

----
# 解析交叉熵计算的具体例子

让我以代码中的例子详细解释交叉熵计算过程，尤其是关于真实分布 $y_i$ 和预测分布 $\hat{y}_i$ 的来源。

## 具体案例分析

在代码示例中：

```python
inputs = torch.tensor([[16833, 3626, 6100],   # ["every effort moves",
                       [40,    1107, 588]])   #  "I really like"]

targets = torch.tensor([[3626, 6100, 345  ],  # [" effort moves you",
                        [1107,  588, 11311]]) #  " really like chocolate"]
```


### 1. 真实分布 $y_i$ 的来源

在语言模型中，真实分布通常是以下形式：

- 对于每个位置，真实分布是一个 one-hot 向量，即在词汇表大小（此例中是50,257）的向量中，只有目标单词的索引处为1，其他位置为0。

例如，对于第一句话的第一个位置，目标是索引为3626的标记（" effort"）：
- 真实分布是一个50,257维的向量，只有第3626个位置是1，其他都是0

在代码中，这个真实分布是隐含的，通过 `targets` 张量指定，表示为每个位置的正确标记ID。

### 2. 预测分布 $\hat{y}_i$ 的来源

预测分布确实是由模型生成的：

```python
with torch.no_grad():
    logits = model(inputs)
probas = torch.softmax(logits, dim=-1)
```


- `logits` 是模型的原始输出（未经过softmax的分数）
- `probas` 是应用softmax后的概率分布，形状为 [2, 3, 50257]（2个句子，每句3个标记，50257个可能的词汇）

对于每个位置，`probas` 中对应的向量就是 $\hat{y}_i$，表示模型预测的概率分布。

### 3. 交叉熵计算步骤

具体计算过程：

1. **提取目标标记的预测概率**：
```python
target_probas_1 = probas[0, [0, 1, 2], targets[0]]
   target_probas_2 = probas[1, [0, 1, 2], targets[1]]
```

   这步从预测分布中提取了真实标记对应的概率值。例如，`target_probas_1[0]` 是模型对第一句话第一个位置预测为标记3626的概率。

2. **计算负对数概率**：
```python
log_probas = torch.log(torch.cat((target_probas_1, target_probas_2)))
```

   对所有提取的概率值取对数。

3. **平均并取负**：
```python
avg_log_probas = torch.mean(log_probas)
   neg_avg_log_probas = avg_log_probas * -1
```

   这就得到了交叉熵损失值。

### 公式与代码对应关系

交叉熵公式 $H(y, \hat{y}) = -\sum_{i} y_i \log(\hat{y}_i)$ 在语言模型中简化为：

$$H = -\frac{1}{N}\sum_{i=1}^{N} \log(\hat{y}_{t_i})$$

其中：
- $N$ 是所有标记的数量（本例中是6个标记）
- $\hat{y}_{t_i}$ 是模型预测目标标记 $t_i$ 的概率

这与代码中的计算完全对应：
1. 提取目标概率 → 获取 $\hat{y}_{t_i}$
2. 取对数 → 计算 $\log(\hat{y}_{t_i})$
3. 取平均并取负 → 完成 $-\frac{1}{N}\sum_{i=1}^{N} \log(\hat{y}_{t_i})$ 的计算

因此，代码中的计算确实是在实现交叉熵公式，其中真实分布是隐含的one-hot向量，预测分布是模型输出的概率分布。

---
您的问题涉及到一个重要的概念澄清。

# 关于真实分布的解释

## one-hot编码与概率分布

当我提到：
> 真实分布是一个50,257维的向量，只有第3626个位置是1，其他都是0

这里描述的是one-hot编码向量，而不是概率分布值为1/50257的情况。

### 区别说明：

1. **one-hot编码**：
   - 在第3626位置的值是1
   - 所有其他位置的值是0
   - 例如：[0, 0, ..., 1(第3626位), ..., 0]
   - 这并不表示均匀概率分布

2. **均匀概率分布**：
   - 如果是均匀分布，每个位置都是1/50257
   - 例如：[1/50257, 1/50257, ..., 1/50257]

## 在交叉熵计算中的含义

在语言模型的交叉熵计算中：

1. **真实标签是确定的**：
   - 我们确切知道下一个标记应该是什么（例如索引3626的标记）
   - 因此使用one-hot编码表示真实分布（确定性分布）
   - 在数学上，这等同于只有正确标记处的概率为1，其他都为0

2. **交叉熵简化**：
   - 当真实分布是one-hot编码时，公式 $-\sum_{i} y_i \log(\hat{y}_i)$ 简化为 $-\log(\hat{y}_t)$
   - 其中 $\hat{y}_t$ 是模型预测正确标记t的概率
   - 这就是为什么代码中直接提取目标标记的概率并取对数

因此，在这个例子中，真实分布不是均匀的1/50257，而是一个确定性分布，用one-hot向量表示，表明我们100%确定正确的下一个标记是什么。

这也解释了为什么交叉熵计算可以简化为仅计算目标标记概率的负对数，而不需要计算所有50,257个可能标记的贡献。